import { AnnaShopValidators } from './anna-shop-validators';

describe('AnnaShopValidators', () => {
  it('should create an instance', () => {
    expect(new AnnaShopValidators()).toBeTruthy();
  });
});
